apiVersion: v1
kind: Service
metadata:
  name: {{ include "magento2.fullname" . }}
  labels:
    {{- include "magento2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "magento2.selectorLabels" . | nindent 4 }}
