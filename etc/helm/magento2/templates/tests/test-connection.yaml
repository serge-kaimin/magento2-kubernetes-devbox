apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pwastudion.fullname" . }}-test-connection"
  labels:
{{ include "pwastudion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pwastudion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
